# 善缘 · 交接 2026-08-17

## 做了什么（本批3个commit·已提交未部署）

| 模块 | 一句话 | 复审 |
|---|---|---|
| chat神秘风页 | chat-magic切正式chat.html·修布局根bug+移动端+合规 | 专家两轮 7→9/10 |
| 八字排盘引擎 | 专业引擎(yiqi-core)进后端替代LLM自排·付费报告排盘不再靠AI猜 | 命理专家满分背书·后端审8.5 |
| 合婚页hehun | 5→9分·修**价格欺诈红线**+国内付款+AI免责+XSS | 独立审8.5 |

排盘引擎在 `server/lib/bazi-engine/`（零新npm依赖）·注入见 `routes/divination.js` 的 `/api/bazi` 与 `/api/bazi/stream`。

## 待办

1. **P0 Karen拍板3件**（合婚页）：①档位=单档¥39.9 还是要¥9.9/¥199三档(要三档得后端加产品) ②「发给TA看TA怎么说」分享金句调性OK？ ③对外价格口径 ¥39.9/$19.90 定稿？
2. **P0 Karen亲测**：微信¥9.9付款全链路（延续0811未做）
3. **P0 部署**：本批3 commit攒够了，HK服务器 `git pull && pm2 restart shenyuan`（Vercel前端另push）
4. **P1 EN/KR/ES/TH/PT 付费报告仍是LLM自排**（未接引擎）——KR사주是主推市场，排期补 `buildBaziBlock` 到各语种handler
5. **P1 引擎静默降级埋点**：`buildBaziBlock` 异常会静默降级回bazi.js简版，加 Sentry `mon` 盯降级率
6. **P1 法律页填Capstone真实信息**（Karen自己填）
7. **P2 hehun `--ink-mid` CSS变量未定义**（回退色·对比度核对）

## 红线 / 坑

- **排错盘=付费红线**：引擎已过命理专家验证(节气定月/子时换日/大运顺逆全对)·**但只中文路径注入了**，其他语种仍AI自排有排错风险(见待办4)
- **价格显示≠实收=欺诈红线**：hehun原来选¥9.9实收¥39.9已修·以后新增档位必须后端有对应产品
- **子时流派**：引擎用「23:00起算次日」·报告已标注·懂行用户按另一流派可能质疑属正常
- **Qwen key/Vercel限额/agora key** 同0811交接不变
- 本地无Qwen key→本地测不了AI出报告(正常)·引擎排盘可本地测：`node -e "require('./lib/bazi-engine')..."`

## 下一步推荐

部署本批→Karen亲测付款+眼过chat/hehun→定合婚档位→跑CMO线路A(八字)+B(合婚)投流测ROI
