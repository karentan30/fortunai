# 善缘 ShenYuan · 交接（0730 晚 · 上线前审查 + 变现闭环）

> 接手先读这份 + `docs/上线前审查报告-0730.md`。本轮主线:上线前审查(3线审计)→修 P0→接通变现闭环→多部门 10 分优化。全部已 commit+push 到 `karentan30/fortunai`(main)。

---

## 0. 快速访问 / 基建
- **生产**:`http://47.242.80.65:3021`(HK · PM2 `shenyuan` · `index.js` 既 serve 前端又是 API · 同源相对路径 `/api`)
- **SSH**:`ssh -i ~/.ssh/id_ed25519 root@47.242.80.65` · app 在 `/opt/shenyuan/`
- **部署**:`bash deploy.sh [backend|frontend|all]`(已修:backend 现 scp `server/*.js` 全部依赖)
- **生产 .env 已配**:`DS_KEY`/`DEEPSEEK_API_KEY`/`STRIPE_PAY_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`FRONTEND_URL`/`ADMIN_TOKEN`
- **架构定死**:API 只在 **HK PM2 单进程**跑(JSON 落盘 `/opt/shenyuan/server/data.json` 有效)。**Vercel 不再跑 API serverless**(vercel.json 已移除 functions)。生产入口用 HK。

---

## 1. 🔴 上线前审查(3 线审计)结论 = 当时不能上线 → 现已修大部分

### ✅ 已修并部署上线(中文主站)
| 项 | 修复 |
|---|---|
| 数据持久化 | `_M` 内存 → JSON 快照落盘,扛 PM2 重启不丢单;`dotenv override:true` 根治 401 |
| **付费墙** | 后端分级 `hasFullAccess`/`gateMessages`:未付费只出 basic 概览(堵 curl 白嫖),完整需 completed 订单;5 报告端点(八字/紫微/合婚/姓名/占星)+ 10 轻端点 |
| **变现闭环** | `bazi.html`:删 Math.sin 假报告 → 真调 `/api/bazi` 出 basic 预览 → 两档付费墙($19.9 完整/$6.9 会员) → 付费回流(存 `sy_last_bazi`+successUrl `?paid=1`→`handlePaidReturn` 带 token 重拉 full) |
| 架构 | 统一 HK PM2(vercel.json 移除 api serverless,防多实例丢单) |
| 安全 | webhook 删无验签降级 / `/api/orders` 加 ADMIN_TOKEN 鉴权 / `/api/order` 金额校验 |
| 防烧钱 | 18 个 LLM 端点加限流 |
| 合规 | 报告 AIGC 标识 + 统一免责 + 健康去医疗化;首页引导+免责+隐私同意+币种统一 $ |
| 韩国支付 | `create-checkout` 加 `region=kr`→KRW+KakaoPay/NaverPay/Samsung/PAYCO(骨架,待 key) |

### 🔴 到"真能上线收钱"还差 3 件(都不在代码)
1. **隐私政策填真实公司信息**(法务红线)——韩语模板已备 `docs/saju-legal-KR.html`(占位 `[회사명]` 等)
2. **真机走一遍付费流程**验证(真卡付款 → 回跳 `?paid=1` → 看到 full 报告)
3. **香港 Stripe key + 确认 Stripe 对"占卜业务"政策**(可能受限,开户前先查)

---

## 2. 🇰🇷 韩国 Phase-0 资产(精美样张 · 未上线)
- `docs/saju-report-KR.html` 深报告 9.6(万字+4图+实践指导+搬家吉日+合规)
- `docs/saju-landing-KR.html` 落地页 10(转化全套件+Kakao share+响应式)
- `docs/saju-legal-KR.html` 法律页模板
- **韩国真上线 = 接线工程**:7 死按钮接真动作 / 建韩语免费结果页 / 报告参数化 + `bazi.js` 引擎韩化 + 韩语 prompt 结构化 / 接韩国支付 / 4 法律页填真。**别按"就绪"预期,预留后端接线周期。**
- 时间点:신년운세 12 月峰 = 倒排期。署名定中性「선연 명리 연구팀」(不打麦玲玲 IP 红线)。

---

## 3. 其他产出(本轮)
- `~/projects/shared/留存OS-作战手册.md`(创始人第五格+读对话 SOP)
- `docs/营销内容包-账号矩阵.md`(4 账号+20 文案+8 视频脚本+发放 SOP)+ `docs/mockups/cards/`(6 张小红书成品卡·本机出图)
- `docs/商业方案-命理师赋能供给引擎.md`("照着念"洞察→供给引擎)
- `docs/支付决策备忘-韩国收款.md`(香港 Stripe 路线+跨境对比)
- `docs/上线前审查报告-0730.md`(P0 分三档)

---

## 4. ⚠️ 两个部署事故教训(务必记住)
1. **deploy.sh 只 scp index.js 漏 pay.js** → 生产 `MODULE_NOT_FOUND` 崩过一次。根因:`pay.js` 之前连 git 都没提交。已改 deploy.sh scp `server/*.js` 全部 + 本次已提交 pay.js。
2. **pm2 `--update-env` 覆盖 DS_KEY → DeepSeek 401**(和历史 401 事故同源)。已用 `dotenv override:true` 根治(.env 永远优先)。**以后重启别用 --update-env**,普通 `pm2 restart` 即可。

---

## 5. 关键文件地图
- `server/index.js` — 后端主体。付费墙 `hasFullAccess`/`gateMessages`(约 line 126-160)、`create-checkout` 韩国支付(约 583)、各报告端点、`pay.js` 引用(line 8)
- `server/pay.js` — 中国支付(微信 NATIVE + 支付宝当面付),生产依赖
- `pages/bazi.html` — 前端变现闭环(`generateReport` 真 fetch、`handlePaidReturn` 回流、两档付费墙)
- `index.html` — 首页(首屏引导+合规)
- 模型路由:审查/CRO/前端优化用 **sonnet** 足够,非架构不动 opus(省 token)

---

## 6. ▶️ 下一步建议(优先级)
1. **中文主站真上线**:填隐私政策 → 真机验证付费闭环 → 配 Stripe key → 上线收钱(最近)
2. 其他付费端点前端也接 token(bazi 已做,紫微/合婚等 pages 页可能需同样加 Authorization)
3. 韩国接线工程(第二城,12 月峰前)
4. 命理师赋能供给引擎(商业方案已备,可先招 5-10 人小范围验证)

*整理:善缘 CEO(Claude)· 2026-07-30 晚*
