# 善缘 ShenYuan 上线完成交接 — 2026-08-10

> **状态**: ✅ 全部就绪，已投放  
> **域名**: https://shenyuan.mylumee.cn  
> **服务器**: 47.242.80.65 · Port 3021 · PM2 online

---

## 今日完成（上线前最终修复）

### 1. AI合规标识 ✅
- **文件**: `pages/tarot.html` / `pages/ziwei.html` / `pages/fengshui.html` / `pages/hehun.html`
- **修改**: `injectDisclaimer()` 加入 `🤖 本内容由AI生成，仅供娱乐参考`
- `bazi.html` + `bazi-en.html` 原本已有，无需改

### 2. geo-fortune GET 404 → 405 ✅
- **文件**: `server/routes/divination.js`
- **修改**: 加了 `router.get('/geo-fortune', ...)` 返回 405 + hint
- 前端本身用 POST 无误，此修复只是让日志更清晰

### 3. Stripe Webhook 注册 ✅（最重要！）
- 原来**完全没配置** webhook endpoint
- 现在已通过 Stripe API 创建：
  - **Webhook ID**: `we_1U2lpYEAXrE2YgcrqFim0ZNL`
  - **URL**: `https://shenyuan.mylumee.cn/api/stripe-webhook`
  - **监听事件**: checkout.session.completed / expired · subscription.created/deleted · invoice.payment_succeeded/failed
  - **Webhook Secret**: 已更新到服务器 `/opt/shenyuan/server/.env`
  - PM2 已用 `--update-env` 重启生效

### 4. Commit & Push ✅
- **Commit**: `162e032` — fix(上线): AI合规标识+geo-fortune 405+博客文章+页面优化
- **Branch**: main → https://github.com/karentan30/fortunai

---

## 当前系统状态

```
服务器    47.242.80.65
PM2       shenyuan · online · port 3021
Stripe    sk_live_* 已连 · webhook已注册
DeepSeek  已连（AI报告生成）
数据库    /opt/shenyuan/server/data.json · 1014KB · 42单历史
磁盘      61%（<80%红线）
SSL       Caddy自动续签 · HSTS已配
```

---

## 健康检查命令

```bash
# 一键检查
ssh root@47.242.80.65 "curl -s http://localhost:3021/api/health"
# 预期返回: {"status":"ok","stripe":"connected","llm":"deepseek"...}

# PM2状态
ssh root@47.242.80.65 "pm2 status shenyuan"

# 最近日志（看有无报错）
ssh root@47.242.80.65 "pm2 logs shenyuan --lines 50 --nostream"

# 查订单数
ssh root@47.242.80.65 "wc -c /opt/shenyuan/server/data.json"
```

---

## 紧急操作

```bash
# 重启服务
ssh root@47.242.80.65 "pm2 restart shenyuan"

# 强制重部署
cd ~/projects/shenyuan && bash deploy.sh all

# 只改后端（快）
bash deploy.sh backend

# 只改前端（快）
bash deploy.sh frontend
```

---

## Karen 待做（仍需人工操作）

| 优先级 | 事项 | 说明 |
|--------|------|------|
| P0 | **真机支付测试** | 打开 `/pages/bazi.html` → 生成命盘 → 点解锁 → 用测试卡 `4242 4242 4242 4242` / 到期 `12/30` / CVV `123` 走一遍。确认收到Stripe邮件确认 |
| P0 | **微信支付真机验证** | 扫码后付款能成功落单 |
| P1 | 配置 UptimeRobot 监控 | 监控 `https://shenyuan.mylumee.cn/api/health`，每5分钟，掉线发邮件 |
| P1 | 韩国KakaoPay | Phase 1规划在 `docs/韩国支付集成-Phase1规划.md`，14-24天上线 |

---

## 产品入口总览

| 市场 | 入口 | 主力产品 |
|------|------|---------|
| 国际/英文 | `/pages/bazi-en.html` | 八字 $9.90 |
| 中文 | `/pages/bazi.html` | 八字 ¥69.9 |
| 韩国 | `/pages/saju-landing-KR.html` | 사주 ₩22,000 |
| 首页（自动跳转） | `https://shenyuan.mylumee.cn` | 按IP语言路由 |

---

## 已知小问题（不影响上线）

- `/api/geo-fortune` 偶尔有 GET 扫描请求，现在返回 405（正常）
- 部分非核心页面无AI标识（非主产品页，后续补）
- 韩国KakaoPay还在Phase 0，韩国用户暂走Stripe
