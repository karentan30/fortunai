# 交接 · 2026-09-07 夜（已上线生产）

## 一句话
夜里把 Runae 三首页 + 报告真实性 + 信任/价格 + 留存 全部优化并**已部署到生产（runae.app 实时生效）**，三件你点名的事全做完，全部 commit+push。

## 现在生产在跑什么
- **分支 `deploy-candidate-0907` @ 4eef35a**（= 我全部夜间改动 + 生产 main 的线上 SEO，零冲突合并）
- 部署方式：`/opt/shenyuan` git reset 到该分支 + `npm install` + `pm2 restart shenyuan`（端口 3021）
- **回滚锚点**：`git reset --hard 3303aa5`（或更早 `a1ad926`）+ `pm2 restart shenyuan`
- 三关冒烟当时全过：首页 200 / 支付返回真 `cs_live` checkout / 生成返回真报告（无假百分比）
- 公网已验证 LIVE：CN 新 hero、EN 新 hero、每日天机留存、report-en $9.90

## 你点名的三件 —— 状态
1. **部署** ✅ 已上线（上面）。当时发现生产 main 与我分支分叉（生产有 4 个线上 SEO 提交），所以走了"合并 main 的候选分支"避免回退 SEO。
2. **防赔钱三洞** ✅ 其实早已实现（架构本就对）：
   - 限频：匿名 30 次/小时、登录 200 次/小时（全局 + 逐路由 `rateLimitMiddleware`）
   - 免费层限次：靠上面的限频；重复不额外生成
   - 付费正文不进未付费 DOM：后端**只给未付费用户生成 3 个免费章节** + `---LOCKED---`，付费章节根本不生成/不下发（server 端 gate，非 CSS 遮罩）
   - ⚠️ 唯一例外：`report-en.html` 是旧的客户端解锁流（localStorage flag），未付费时正文虽是 server 只给免费段，但解锁逻辑在前端——它是次要落地页，建议后续统一到主流程或加 server 校验。
3. **report-en $9.90** ✅ 修了一个**真金 bug**：原来显示 $2.99 却按 store 扣 $7.90（超收/拒付风险）。已统一 store `report_unlock_a` 790→990 + 页面全部 $2.99→$9.90（显示=扣款）。tier b $4.99 与 store 一致未动。

## 全部夜间改动清单（都已上线）
- 三首页转化重构（CN 6.2→~8.3 / EN 5.5→~8.0 / ColorDNA 6.8→~8.4，2 轮产审 loop + 回归修复）
- 报告去"一眼假"（假百分比/评分/幸运日/讨论区/倒计时/合婚分 floor/hex 色）
- 价格全站 $9.90 一致 + 删假承诺/假原价 + 品牌 RUNAE 统一 + report-cn 合规免责
- 每日 streak IP 污染 bug 修复
- **生辰透传免重输 + 每日天机回访免重输 + 跨页 sy_birth 记忆**（留存最高 ROI）
- account.html 红→品牌玉/金
- 每日巡检 cloud routine（明早 9:04）

## "8 分怎么做到 10 分"——诚实差距分析
文案/结构/信任我已做到 ~8-8.5（这是 copy+markup 能到的上限）。剩下 1.5-2 分**需要素材/数据/设计，我一个人夜里做不了**：
1. **真实社会证明**（差最多）：真实用户好评/评分/媒体提及。**禁造假**——有一条真的都比编的强。你有种子用户就给我 2-3 条真话，我接上立刻 +1 分。
2. **专业级视觉/"wow"时刻**：首页需要一个截图级惊艳的命盘可视化 / hero 大图。生图要花钱（需你批预算），且按你规矩要先出概念图审。EN 报告接 `report-viz.js`（五行环/大运时间轴）能大幅提升"物有所值"感——这个我可以做（不花钱），是下一个高 ROI。
3. **落地页的情绪峰值**：CN 首页可加一个"输入生辰即出一句专属钩子"的即时惊喜（类似 ColorDNA 的揭晓感）——需要设计确认再做。
4. **移动端真机走查**：我只能静态验证，真机滚动/点击热区/加载得你或我用浏览器工具过一遍。

→ 想冲 10：先给我几条真实好评 + 批 report-viz 可视化接入（免费）+ 定一个 hero 视觉方向。

## 其他可继续做（下一批，安全非付费）
- 报告接 `report-viz.js` 真渲染（付费感 +，免费）
- report-en 统一到主流程 or 加 server 端解锁校验（补上 2c 例外）
- 免费计算器 hour 编码与报告页统一（现在跨页不传 hour 是权宜）
- order.html 直接访问兜底、深/浅背景跨页统一
- 裂变奖励闭环真接线（分享卡出图 + 奖励到账展示）

## 分支说明
- `deploy-candidate-0907` = 当前生产 = 单一事实源，继续在这上面迭代 + 部署最省事。
- `launch-en-shop` = 我夜间的开发分支（已并入候选）。
- color-dna repo：英文首页改动在 main（Vercel，未在本次部署范围）。
