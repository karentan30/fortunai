# Runae 交接 · 2026-08-20（八字外方法收口到"$9.9 能卖"）

> 接手先读基础：`docs/交接-Runae-0819-接手先读.md` + `docs/交接-Runae-0819晚-八字到10分+全站.md`。本篇=这一轮增量。
> 已部署验证。commit `565fc22`，push origin/main，server `/opt/shenyuan` git reset origin/main + `pm2 restart shenyuan --update-env`。线上 https://runae.app 200。

## 一句话
把 **紫微/手相/合婚** 从"技术能收钱"做到"干净 $9.9 能卖"并 curl 真验证；全站价格统一 $9.9；西占/九星留下一轮。**唯一真缺口仍是 distribution（≈0 用户）。**

## 做了什么（已验证）
- **手相**：堵付费白送红线（`pages/palm-reading.html` 新增 `freePortion()`，免费预览截到 `---LOCKED---`/首个付费章 emoji 前）+ CTA 接真结账 `shouxiang_full`（原来跳错 bazi-en.html）。store.js 新增 `shouxiang_full` 产品 + UNLOCK 映射。
- **紫微**：`server/routes/divination.js` `/ziwei/stream`(2080·线上用) 去掉 17 个数字编号维度（数字编号违反 FMT_LAW → 被前端切章器误切成空白卡）、system 挂 `${FMT_LAW_ZH}`、走 `priority:'deepseek'`、免费预览 🌙 段加强悬念钩子；前端 `pages/ziwei.html` 加 P1 兜底防白送 + 收紧切章器（只认大标题 emoji+≤24 字）。
- **合婚**：`pages/hehun.html` 3 档（基础/完整/大师批婚含真人连麦）→ 单一 $9.9；真人连麦下线。
- **价格统一 $9.9**：5 城市 LP($19)/lp-master-EN($4.99)/lp-bazi-geo(¥99)/lp-master-CN/pricing.html 全转 $、多档砍单档。
- store.js 定价：`ziwei_full`/`hehun`→990（$9.9）。三者结账都验证生成 live Stripe URL。

## 🔑 关键机制（省事·非显然）
结账支持**动态 price_data**（`server/routes/payment.js:210-224`）：product 无固定 Stripe price ID → 按 `PRODUCTS[product].amount`(分) 动态收费，权益按 product 字符串授予（store.js `UNLOCK_BY_CATEGORY`）。**所以让任何方法 $9.9 只需改 store.js amount=990，无需去 Stripe 后台建 SKU。** 只有 bazi_full/hehun_full/member_* 有固定 price ID（固定价优先）。

## 🔴 待办（下一轮）
1. **西占(astrology.html/western-astrology.html) + 九星(kyusei.html)**：product/UNLOCK 没接全（kyusei 完全无 product，'astrology' product 不在 PRODUCTS）。补 product(amount:990) + UNLOCK_BY_CATEGORY + 前端 CTA 接线 + 验证，才算能卖。
2. **手相真机 e2e**：后端+前端都改好，但没传真手掌图跑过。Karen 传一张手掌照端到端验一次。
3. **distribution**：产品够用了，劲砸获客。下一步=解签×小红书第一条（素材 `docs/获客-第一批内容-给运营.md`）。

## 坑/红线
- 改紫微内容只动 `/ziwei/stream`(2080)，别动没用的 `/ziwei`(998)。后端改必 `pm2 restart shenyuan --update-env`。
- **新章节名要同步前端 emoji 白名单**（ziwei.html 切章正则 ~441，八字铁律：禁 emoji/数字/圈数字做正文小标题）。
- `/api/products` 读 amount 字段：bazi_full amount 仍 2900 显示 $29（但真收 $9.9 走固定 price）。若有页面读 /api/products 会显错——可把 bazi_full amount 也改 990 对齐（固定 price 不受影响）。
- 合婚 hehun.html `showCnPayModal` 的 ¥ 是国内微信/支付宝真实结算金额，别改成 $。
- 紫微 cosmetic：模型偶把章节 emoji 与标题换行分开（标题略空），可在 prompt 强调"emoji 与标题同一行"。
- 改后端后**别只看部署成功，必 curl 真验生成**（本轮紫微/手相/合婚都真跑过）。

## 北极星
产品又擦亮一截，真缺口没变 = distribution（≈0 用户）。报告再好 × 0 用户 = 0。
