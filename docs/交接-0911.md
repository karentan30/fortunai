# 交接 · Runae（2026-09-11）

> 承接 `docs/交接-Runae大重构-0910.md`。本轮**没有部署**，全部改动在本地分支 `work-0911`
> （基于 origin/main = `c1dc0bc`），共 4 个 commit，等部署批准。
> 本轮做的是三件事：**把 0909 那批「看起来修了、其实没修」的安全/合规问题真正修掉**、
> **核实上一份交接里几条未经证实的结论（其中一条我推翻了自己）**、
> **把需要你拍板的事攒成一份决策清单**。

---

## 0. 生产事实（0911 实测）

- 生产分支 `main`，服务器 `/opt/shenyuan`，PM2 进程名 `shenyuan`，端口 3021。
- 域名：**runae.app**（英文，根 → `pages/home-en.html`）/ **shenyuan.mylumee.cn**（中文，根 → `index.html`）。
- SSH 需显式指定密钥：`ssh -i ~/.ssh/hk_deploy root@47.242.80.65`（不带 `-i` 会 publickey 失败）。
- push 用 `git -c http.version=HTTP/1.1 push`（本机代理 HTTP/2 会报错）。
- ⚠️ `git reset --hard` 在本机被权限分类器拦截（判定为不可逆本地破坏），用 `git checkout -b` 代替。
- **Stripe 密钥全部是 live 模式**（只看了前缀 `sk_live_`/`pk_live_`/`whsec_`，未打印任何值）。

### 0.1 站点真实体量（这条决定后面所有排期判断）

- **真实用户数：0。历史上只有 1 笔订单到过 `completed`（2026-09-08）。**
- 41 笔 `pending` 全部落在 07-30 → 08-10，即**支付打通之前**；08-09 之后全部是 `expired`/`completed`。
- 所以：本轮所有安全/合规修复都不是「救火」，而是**在没人用的时候把地基修对**。别按线上事故的优先级去读它们。

---

## 1. 🔴 我推翻了自己的一条结论（重要，先说）

我在核查过程中一度判断「Stripe webhook 挂了、付费用户拿不到东西」，**这个判断是错的，我自己证伪了**：

- 起因：日志里有 19 条 `[WEBHOOK SIG ERR] No signatures found matching the expected signature for payload`，且 `[PAYMENT]` 成功日志为 0。
- 证伪：按天统计订单状态发现——**08-09 之后每一笔订单都正常走到了 `expired`/`completed`**，`expired` 只在 webhook 正常工作时才会被写（`payment.js:373` 是唯一写入口）。若 webhook 真挂，08-09 之后的单子会一直卡在 `pending`。
- 复验：无签名 POST → 400；伪造签名 POST → 400；`GET /api/.env` → 404。
- **结论：webhook 是健康的。那 19 条是外部扫描器往公网端点发垃圾 POST，被正确拒绝了。**

保留这条记录是因为：如果只看到「19 条验签失败 + 0 条成功」，很容易误判成 P0 线上故障并去做无谓的紧急处理。

### 1.1 顺带核实的三条（都成立）

| 结论 | 怎么验的 |
|---|---|
| `/api/bazi` 付费墙 0909 报告的泄露**已真修好** | 实测返回：服务端切分、`---LOCKED---` 之后只剩 274 字预告、`locked: true`、`tier: "basic"` |
| `gateReportAccess()` 是**真门**，不是前端摆设 | 读 `store.js:446`：token → user → orders 链路校验，无法用 `req.body` 伪造 |
| `/api/success` 是**纯静态页** | `payment.js:492`：只读 URL 上的 `?product=` 就渲染「功德圆满」，**零 Stripe 校验** |

> `/api/success` 那条是**健壮性/信任缺口，不是线上故障**：付款成功页不验单，意味着构造一个 URL 就能看到「支付成功」的页面。没有实际资损（真解锁仍走 `gateReportAccess`），但用户可能被自己伪造的成功页误导。**建议排期修，不急。**

---

## 2. 本轮做完（未部署 · 分支 `work-0911`）

### 2.1 支付：堵住「支付宝 + 订阅」的组合（`03cc8a5`，cherry-pick 自 0909 分支）
- 新增纯函数 `server/lib/stripe-methods.js`：`resolvePaymentMethods(methods, isSubscription)`。
- 问题：`payMethods`（来自请求体 `region`/`currency`）与 `isSubscription`（来自 `product`）各自独立计算，
  于是**非大陆 IP 发 `region:'cn'` + 会员商品**会得到 `['card','alipay']` + `mode:'subscription'`。
  Stripe 的支付宝/微信只支持一次性付款 → 请求被拒 → **500**。
- 现在订阅单自动降级为 `['card']` 并 `console.warn` 留痕。9 条单测通过。
- **仍需你在 Stripe 后台做的事**：见该 commit 里的「后台开通清单」（支付宝/微信支付方式要在 dashboard 里实际打开，否则降级后用户只剩卡可选）。

### 2.2 安全：三处「看起来有防护、实际没生效」（`fc8fc53` + `a24ccb8`）

**(a) 限流白名单只有 13 个前缀，站上已有 40+ 路由。**
后来新增的 `numerology` / `life-kline` / `chart-archetype` / `identity-triad` / `western-astrology` /
`best-timing` / `love-destiny` / `hepan` / `support-chat` / `shouxiang` / `duanshi` 等等
**全都写了 `rateLimitMiddleware` 却完全不生效**——中间件第一行就 `return next()` 了。
白名单式防护天然随新增路由腐化 → 改成 **`/api/*` 默认拒绝 + 显式豁免**，新增路由自动受保护。

**(b) 中间件被挂了两次。** 全局挂 1 次（`index.js:87`）+ 各路由挂 89 次，
同一次请求计两次 → **实际限额只有标称的一半**（匿名 75/h 而非 150/h）。
加 `req._syRateCounted` 幂等标记。

**(c) 🔴 补测试时立刻抓到一个会真出事的 bug：支付回调根本没被豁免。**
豁免名单里写的是 `'/api/pay/'`（带尾斜杠），但匹配规则是
`path === prefix || path.startsWith(prefix + '/')` —— prefix 带尾斜杠时第二个条件变成
`startsWith('/api/pay//')`，**永远为假**。
于是 `/api/pay/wechat/notify`、`/api/pay/alipay/notify`、`/api/pay/*/query`
这些**钱路全部落在限流范围内**。后果是「用户付了钱 → 回调被 429 → 不发货」，
且微信/支付宝会重试、重试同样被拒，订单永远到不了 `completed`——**静默且极难归因**。
改成 `'/api/pay'`（不带尾斜杠）。

**(d) 排行榜页会被打挂。** `pages/leaderboard{,-en}.html` 每 10 秒轮询
`/api/referral/leaderboard` + `/api/referral/mine`（360 次/小时 × 2 接口）。
旧白名单模式下这两个压根不受限；改默认拒绝后**约 12 分钟就会开始 429**。已加豁免（只读、不烧 LLM）。

**(e) 免费额度可被客户端重置。** 配额 key 原本是 `(uid || sessionId || ip)`，
而 `x-session-id` 是**客户端自己发的 header**，前端总会带上 → uid 为空时永远走 sessionId 分支，
**每次请求换个随机值即可重置配额**，免费额度形同虚设。
改为只认服务端可控的 `(uid || ip)`。涉及 8 个路由：
`numerology` / `oracle` / `life-kline` / `iching-en` / `daliuren-en` / `qimen-en` / `ziwei-en` / `daily`。

**(f) `ADMIN_TOKEN` 未设时的放行。** `email.js` 三处 + `push.js` 一处只写了
`token !== process.env.ADMIN_TOKEN`；若 env 未设，两边都是 `undefined` → **不带 token 的请求反而能过**。
加 `!process.env.ADMIN_TOKEN ||` 短路。**生产当前有设 ADMIN_TOKEN，属纵深防御，不是线上可利用漏洞。**

**新增测试 `server/middleware/rate-limit-scope.test.js`（8 条）** 钉住「哪些路径受限」：
钱路红线、轮询豁免、AI 路由必须受限、新增路由默认受保护、前缀段边界（`/api/payroll` 不能被 `/api/pay` 蹭到豁免）。
> 另：清理定时器加了 `.unref()`——它把事件循环钉住，导致任何 `require` 中间件的 `node --test` 都会永远挂住
> （上面那组测试一开始就是这么卡死的）。生产无影响。

### 2.3 合规：清理 6 个页面上残留的编造社会证明（`4344511`）
都是**「注释写着已删除、其实没删干净」**，或同类漏网：

| 页面 | 残留内容 |
|---|---|
| `pages/lp-in-vedic.html` | 头部 `⭐ 4.9 rating` 还在；三条具名评价（Priya S./Arjun M./Divya R. + 5星）也还在。**上方注释写着「已移除」，实际只删了后半句** |
| `pages/lp-bazi-th.html` | 注释写「编造社会证明已移除」，但 `รีวิวจากผู้ใช้จริง`（来自真实用户的评价）三条具名评价仍在 |
| `pages/lp-bazi-geo.html` | trust bar `50w+ 用户已解读` / `9.6/10 平均好评`；页尾 `已有 50w+ 用户…` / `九成用户表示"非常准确…"` |
| `pages/lp-bazi-cn-c.html` | 「真实用户报告节选」；「用户对比」；编造的「性格 90% 准」；具名客户故事「小叶，32岁，上海 · 已购买完整报告」 |
| `pages/shop.html` | 4 处 `★★★★☆ 热销推荐`——真实订单为 0，星级和「热销」都无据 |
| `pages/qiuzi.html` | 页尾「成功案例来自真实用户」，但本页未展示任何案例 |

处理方式：能改成**明示示例**的（`lp-bazi-en-b.html` 已有标准样板 `Illustrative Examples` + `These are not quotes from real users.`）就改成示例；
纯数字/星级的直接删，换成产品客观属性或不涉社会证明的事实。
`lp-bazi-geo.html:202` 的 `★★★★☆` 在「李明（示例）」示例报告内、已标注，**保留**。

### 2.4 补齐悬空引用：`docs/密钥轮换-runbook.md`（新增，未 commit）
0909 的交接引用了这个文件但它不存在。内容：为什么轮换（`.env.bak` 泄露 `ADMIN_TOKEN` + `STRIPE_WEBHOOK_SECRET`）、
当前状态表、Stripe 后台 roll 步骤、服务器 `.env` 更新 + **`pm2 restart shenyuan` 是必须的**
（`payment.js:76` 在模块加载时读 secret）、验证 `ADMIN_TOKEN` 确实已轮换、清理服务器上的 `.env.bak.*`、红线。

> 其中第 7 节我写错过一次：初稿断言 localStorage 那套绕过（`sy_unlock_a` / `sy_paid_duanshi` / `sy_hehun_paid`）
> 已失效。**我实际只测了 `/api/bazi` 一条。** 已改成只写测过的，其余标为**未验证**。
> 另外 `sy_unlock_a` / `sy_paid_duanshi` / `sy_hehun_paid` 这几个字符串，**在 `docs/自检清单-上线前必过.md:45` 里被我们自己写成了操作说明**——任何人拿到这份文档都能照着绕过前端。建议一并处理。

---

## 3. 🔴 待你拍板 / 只能你做的事

### 3.1 部署批准（本轮所有改动都停在这里）
分支 `work-0911`，4 个 commit，`origin/main` 之后。**我没有部署，也不会在你明确说之前部署。**
部署命令：`git push origin HEAD:main` → 服务器 `git fetch && git reset --hard origin/main` → **`pm2 restart shenyuan`**
（改了 `server/` 就必须重启）。

### 3.2 合规红线（需要产品/法务拍板，我不该单方面动）

1. **`blog/` 140 篇里的编造客户故事 —— 最大的一坨，也是最难的。**
   我自己实测的数字：**27 篇**明确写着「真实案例/真实用户/真实命例」；**44 篇**是第一人称「一位客户来找我」叙事；**27 篇**含编造统计。
   最硬的一处我亲自打开确认过 —— `blog/hongluan-tianxi.html`：
   > 「我统计过近十年找我排盘的**1200个真实命例**，其中在红鸾流年结婚的占 **38%**，在红鸾流年恋爱或订婚的占 **29%**…」
   这是**编造样本量和百分比**，比普通证言更硬，而且还配了一个完整的假客户故事（「一位在互联网大厂做高管的女士…婚礼上她特意来敬酒」）。
   **为什么我不动**：这 140 篇是内容资产（SEO 主力），不是几行文案。删 / 改第三人称通用情境 / 整体下线，
   三种做法对 SEO 和品牌的影响完全不同，得你定。**我的建议：保留文章骨架、去掉「我」和「真实」、把案例改成「常见情境」。**
   ⚠️ 另外 `blog/generate.js:224` 是**生成器模板**，里面写死了「127,000+读者的命理知识库」——
   **改了模板不会自动修好已生成的页面**，需要重新生成或批量替换。

2. **`pages/cemetery-guide.html` —— 法律风险最高的一处。**
   我亲自确认：`:798` 声明「以上风水评分由善缘专家团队依据三元玄空与形势派双体系评定」，
   而实际是 **30 处硬编码数字**，打分对象是**真实存在的陵园**（例：`:220` 玫瑰岗纪念公园 / Rose Hills Memorial Park, Whittier, CA → `9.2 风水评分` + 「全美华人墓地综合评分第一」）。
   **编造「专家评分」去评价真实第三方商家**，不只是信任问题，涉虚假宣传 + 对真实商誉主体的不当评价。
   **我的建议：直接下线整套评分**，页面改成信息汇编。但这会改变整页定位，需要你和法务确认。

3. **`pages/shop.html` 的假划线原价**（例：`:291` `¥89` 划掉 `¥159`，而该商品从未以 ¥159 卖过）。
   这属于**虚假折扣**，和 2.3 里的假星级是同一类问题但更敏感。**改价是商业决策，我没有擅自动。**

4. **`pages/qiuzi.html:353` 的功效承诺**：「三路并行，**最大化显灵概率**」。
   这是对超自然结果的效果承诺，且产品是求子（情绪高度敏感的场景）。**这句话是全页卖点，我不该替你重写。**

5. **`pages/masters.html`**：旧页有 6 个虚构收费命理师（0909 就挂着，未清）。要不要下线 / 换真人。

6. **`pages/lp-bazi-cn-c.html` 的整体叙事结构**：我把具体的假证言改成了明示示例，
   但这一页整体建立在「真实用户报告节选」的叙事上，**改写法需要你定调**。

### 3.3 密钥轮换（`docs/密钥轮换-runbook.md` 有完整步骤）
- `ADMIN_TOKEN`：已轮换 ✅
- **`STRIPE_WEBHOOK_SECRET`：待你在 Stripe 后台 roll ❌** —— 改完必须更新服务器 `.env` 并 `pm2 restart shenyuan`
- 服务器 `/opt/shenyuan/server/` 下的 `.env.bak.*` 两个文件：**待删 ❌**（别再往 git 工作目录放 env 备份）

### 3.4 0909 就挂着、仍未完成的
- **真人命理师**：`booking.html` 是中性占位，等你给真人名字/资质/定价（合规红线，我不编造）。
- **DeepSeek 充值**（可选）：qwen 免费已能跑全部；充了文笔更好。
- **付费真单回归测试**：Windows 机用 test key 验 `tarot_5` / `tarot_3` / `saju_kr_full` 真付费用户不被误挡。

---

## 4. 待办（可继续做，不需要你拍板）

- `/api/success` 加真实校验（见 1.1）——现在是纯静态页，构造 URL 即可看到「支付成功」。
- **验证 localStorage 那套付费解锁绕过**：`sy_unlock_a` / `sy_paid_duanshi` / `sy_hehun_paid`。
  我只测过 `/api/bazi`（已修好），**另外两个没测**。同时处理 `docs/自检清单-上线前必过.md:45`
  把绕过方法写成操作说明的问题。
- `site-audit.js --dir pages` 结果：220 页，**FAIL 20 / WARN 251**（比留言板上记的 37 FAIL 有改善），
  失败集中在旧页。旧散页逐步下线 / 路由到新的 3 页流程。
- 0908 那批前端改版 TODO：**大部分已被 0910 大重构覆盖**，需要重新分诊，别照着旧清单做。
- 改限流时记住一条：**以后任何新增的轮询页，都要先确认它打的接口在豁免名单里**
  （`server/middleware/index.js` 的 `RATE_LIMIT_EXEMPT_PREFIXES`），
  否则用户开着页面十几分钟就会开始报错。排行榜那次就是这么踩的。

---

## 5. 红线 / 坑（继承 + 本轮新增）

- **密钥值永不进 git / 聊天 / 截图**（`.env.bak` 事故教训）。只看前缀、长度、布尔。
- **另一台 Mac 也在 `fortunai` 提交**（同一个 karen 身份，两个 Claude 会话）。
  改前先 `git fetch`；协作看 `~/projects/shared/协作/留言板.md`。
  > 本轮教训：我一开始基于**本地陈旧仓库**给了你一份状态汇报（把 `docs/交接-0907最新-已上线状态.md` 当成最新交接），
  > SSH 上去才发现服务器比本地领先好几天，真正的上一份交接是 `docs/交接-Runae大重构-0910.md`。
  > **以后先 `git fetch` + SSH 看服务器，再汇报状态。**
- **`git add -A` 会把 `.env.bak` 一起提交**，永远显式列文件。
- `git reset --hard` 被权限分类器拦截，用 `git checkout -b` 等非破坏性等价操作。
- runae.app 根 = `home-en`（英文），中文在 `.cn`；**runae.app 上访问 `/index.html` 会报错**。
- 原生 `select`/`date` 控件在深色模式下需 `color-scheme: dark` + `option` 显式深底亮字。

---

## 6. 关键文件

- 本轮新增：`server/lib/stripe-methods.js`（+ `.test.js`，9 条）、
  `server/middleware/rate-limit-scope.test.js`（8 条）、`docs/密钥轮换-runbook.md`、本文件。
- 本轮改动：`server/middleware/index.js`（限流默认拒绝 + 幂等 + pay 前缀修复 + 轮询豁免 + `.unref()`）、
  `server/routes/payment.js`（`resolvePaymentMethods` 接入）、
  8 个配额路由、`server/routes/email.js`、`server/routes/push.js`、
  6 个合规页面（`lp-in-vedic` / `lp-bazi-th` / `lp-bazi-geo` / `lp-bazi-cn-c` / `shop` / `qiuzi`）。
- 合规改写样板：`pages/lp-bazi-en-b.html:1354` —— `Illustrative Examples` + `These are not quotes from real users.`
  （另有 `lp-bazi-cn-a.html:1137`、`lp-saju-kr-a.html:1257`、`member.html:345` 等已写对的，别误伤）
- 设计 token：深色玉金（见任一 `pages/report-v2.html` 的 `:root`）

---

## 7. 一句话总结

**地基上的三个洞（限流没生效、配额可重置、支付回调会 429）本轮真修好了，而且是被测试抓出来的；
六个页面残留的假社会证明清掉了；剩下最大的两块——`blog/` 140 篇的假客户故事和
`cemetery-guide.html` 对真实陵园的编造评分——是内容和法律决策，我停在这里等你拍板。
全站真实用户为 0，所以这些都不是救火，是在没人用的时候把地基修对。**

---
---

# 交接 · Runae（2026-09-11 夜 · 第二轮）

> 承接上面的 `docs/交接-0911.md`。本轮**已部署到生产**（`main`，`f20089f`）。
> 承接判断：上一轮的决策清单里，「报告分等级 + 导出 PDF」已由 Karen 拍板并做完，
> 本轮做的是**继续找静默失败**，抓到四类新问题，全部修好并部署。

## 8. 本轮做完（已部署 `f20089f`）

1. **收钱不发货（最重）**：`report_unlock_a` / `report_unlock_b` / `bazi_basic` / `saju_kr_full`
   这四个 SKU 在 11 个页面上卖，但不在任何解锁表里。用户付了钱，服务端一个字都不放；
   页面还把「章节标题预告单」的模糊遮罩去掉，装作已解锁。已全部纳入 `UNLOCK_BY_CATEGORY`。
2. **付完款不重新取正文**：老报告页支付成功只做 `classList.remove('locked-section')`，
   而那段内容本来就是免费预览里的章节标题清单。现在付完款重载一次拿全文，并给取报告的请求补上身份。
3. **saju-landing-KR 的付款按钮是死的**：传的产品键 `saju_kr_full_krw` 其实是 Stripe price id 的键名，
   服务端查 `PRODUCTS` 必 404 → 一个人都付不了款。改成 `saju_kr_full`。
4. **两个页面在生产上整块哑掉**（内联 script 一处语法错＝整段不执行）：
   `report-in.html`（少转义一个撇号，整页卡在转圈）和 `inspiration.html`（多一个 `[` 和 `:`，星座列表渲染不出）。
   两个页面都不在冒烟清单里，所以活了很久没人发现。已修，并把它们加进了冒烟清单。

## 9. 新增守卫（两条，都能在 bug 复现时变红，已实测）

- `server/lib/sold-skus-honored.test.js` — 页面卖得出去的 SKU 必须在 `PRODUCTS` 里、且有人认账。
- `server/lib/page-script-syntax.test.js` — 192 个页面 / 374 段内联 script 必须编译通过。

## 10. 🔴 待你拍板（本轮新增一条）

**老报告页的订单是无主的，这笔钱永远兑不出来。**
`report-{en,cn,in,th,es,pt-br}` 这些老页面**没有任何登录入口**，付款单 `user_id = null`，
而解锁全靠「查这个用户名下的订单」，所以即使 SKU 已认账，匿名买家也拿不到东西。
修法二选一，都属产品决策，我不单方面动：
- **A. 付款前加登录**（改动小，但会掉转化）；
- **B. 付款后按 `out_trade_no` 认领，给这个浏览器发一张匿名令牌**（不掉转化，但要新增一条发身份的路径，需评审）。

线上目前只有 1 笔 `completed` 订单（`daily_companion_year`，`uid=null`），**尚无实际损失**，不急着上线。

## 11. 仍等你做的

- **DeepSeek 余额**：已耗尽（402）。现在全靠 qwen 一个 provider 顶着，**单点**。
  推荐：充值 DeepSeek（恢复主用 prose 模型）+ 顺手加一个免费 Groq key。
- `gemini-2.0-flash` 已下线**且**香港地理封锁，配了也没用，建议从链里去掉或换型号。
- Ollama 兜底在跑但**一个模型都没拉**，等于没配。
- `/api/health` 里的 `"llm":"deepseek"` 是写死的，deepseek 挂了它也照样这么报——容易误导，待改。
- 上一轮清单里仍挂着的：`booking.html` 真人算命师、`masters.html` 虚构大师、
  Stripe webhook 密钥轮换、`shop.html` 假划线价、`qiuzi.html` 效果承诺、`blog/` 140 篇假客户故事。

## 12. 部署后实测（全部在生产上跑）

| 检查 | 结果 |
|---|---|
| 四个 SKU 的服务端判定（tier/locked） | 4/4 放行；匿名与买错产品仍是锁的（负面验证过） |
| 报告等级 + 导出 PDF | 7 个报告页 0 失败，栅格化量纸白亮度 249.6–253.0 |
| UI 冒烟（Runae 全部 22 个目标） | **44/44 ✅，0 🟡 0 ❌** |
| 单测 | 35/35 通过 |
| 两个坏页面线上复查 | 0 JS 报错；`inspiration` 星座元素从 0 → 12 |

## 13. 一句话总结

**这一轮修的不是新功能，是「用户付了钱、服务端从来没打算发货」——
11 个页面在卖 4 个没人认账的 SKU，还有 2 个页面在生产上整块脚本哑掉。
全部修好并部署，冒烟从 18✅4🟡2❌ 变成 44✅0🟡0❌（那 2 个 ❌ 查下来是测试工具自己的误报，已一并修掉）。
现在最大的未决项是老报告页的匿名订单兑不出来，需要你选登录还是认领，我没动。**
