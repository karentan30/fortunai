# 善缘 ShenYuan 上线冲刺交接 — 2026-08-10 晚

> **状态**: ✅ 生产就绪，全部修复已部署  
> **域名**: https://shenyuan.mylumee.cn  
> **GitHub**: https://github.com/karentan30/fortunai (branch: main)  
> **最新 commit**: d86f8f7

---

## 今日完整改动清单

### 本次 commit (d86f8f7)
| 文件 | 改了什么 | 目的 |
|------|---------|------|
| `pages/bazi.html` | 付费墙加竞品价格对比锚点 | 提升转化：¥500线下大师 vs $9.9善缘，锚定用户心理 |
| `server/routes/payment.js` | Stripe Price ID 修正 + 新增 CNY 路由 (alipay) | bazi_full 现在正确映射到 Stripe；中文用户可用支付宝 |
| `server/routes/referral.js` | `GET /api/referral/share-card` SVG 分享卡 | 用户邀请时生成带二维码/邀请码的精美卡片 |
| `pages/report.html` | `special-insight` 区块 CSS | 报告页"特殊洞见"时间区块样式 |
| `pages/saju-legal-KR.html` | AI声明 + 日期 + 관할법원 + 개인정보책임자 | 韩语法律页占位符全部填完，合规上线 |

### 上午 commit (162e032)
| 改动 | 详情 |
|------|------|
| AI合规标识 | tarot/ziwei/fengshui/hehun 的 `injectDisclaimer` 加"🤖本内容由AI生成" |
| geo-fortune 405 | GET 请求返回 405 Method Not Allowed，不再 404 |
| 博客文章 40 篇 | 中/英/韩 SEO 博客，sitemap 已更新 |

---

## Stripe 配置（今日完成）

| 项目 | 状态 | 详情 |
|------|------|------|
| Webhook endpoint | ✅ 已创建 | ID: `we_1U2lpYEAXrE2YgcrqFim0ZNL` |
| Webhook URL | ✅ | `https://shenyuan.mylumee.cn/api/stripe-webhook` |
| 监听事件 | ✅ | checkout.completed/expired · subscription · invoice |
| Webhook Secret | ✅ 已同步 | 服务器 `.env` + 本地 `.env` 均已更新 |
| Price IDs | ✅ 修正 | `bazi_full` → `price_1TzAjGEAXrE2YgcrRzUY78Ko` |

---

## 服务器当前状态

```
PM2        shenyuan · online · port 3021
Stripe     sk_live_* 已连 · webhook已注册已激活
DeepSeek   已连（AI报告）
数据库      /opt/shenyuan/server/data.json · 已有 42 单
磁盘        61%
```

### 健康检查
```bash
ssh root@47.242.80.65 'curl -s http://localhost:3021/api/health'
# 预期: {"status":"ok","stripe":"connected","llm":"deepseek"...}
```

---

## Karen 还需亲自做的（仅剩2项）

### P0 — 真机支付测试（10分钟）
```
1. 打开 https://shenyuan.mylumee.cn/pages/bazi.html
2. 输入生日 → 生成命盘
3. 点「立即解锁完整命盘 · USD $9.90」
4. 填测试卡: 4242 4242 4242 4242 / 12/30 / 123
5. 确认: Stripe 发确认邮件 ✅
```

### P1 — UptimeRobot 监控（5分钟，免费）
```
监控 URL: https://shenyuan.mylumee.cn/api/health
检查间隔: 5分钟
告警: 邮件 → tan42204@gmail.com
```

---

## 下一步路线图

| 时间 | 事项 |
|------|------|
| 今晚 | Karen 真机支付测试 + 配 UptimeRobot |
| 明天 | 投放 TikTok/小红书（素材包在 `docs/交接-TikTok生产-0810.md`）|
| 8月底 | 韩国 KakaoPay Phase 1（规划: `docs/韩国支付集成-Phase1规划.md`）|

---

## 紧急操作速查

```bash
# 重启服务
ssh root@47.242.80.65 "pm2 restart shenyuan"

# 重部署（前端+后端）
cd ~/projects/shenyuan && bash deploy.sh all

# 查最新日志
ssh root@47.242.80.65 "pm2 logs shenyuan --lines 50 --nostream"

# 查今日订单
ssh root@47.242.80.65 "curl -s -H 'x-admin-token: karen_ceo_shenyuan_2026' http://localhost:3021/api/orders | python3 -c \"import sys,json; orders=json.load(sys.stdin)['orders']; print(f'总单数: {len(orders)}')\""
```
