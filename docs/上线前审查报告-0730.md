# 善缘 上线前审查报告（0730 · 三线并行审计）

> 结论:**当前不能上线。** 分两个层面看,别混为一谈:
> - **A. 中文主站(已上线 HK 3021)** 有真实后端安全/合规 P0 —— 这是**正在收钱的产品**,漏洞紧急。
> - **B. 韩国资产(saju-report / saju-landing)** 是 **Phase-0 样张**:设计/文案到 10 分,但**工程未接线**,点按钮没反应、输生日不出结果、报告是死数据、收不到韩元。不是上线品,是原型。

---

## ✅ 本次已立即修复并部署(中文主站)

| 项 | 修复 |
|---|---|
| Stripe webhook 无验签降级 | 删除 else 分支,无 secret 直接拒(防伪造付款刷单) |
| `/api/orders` 无鉴权泄露全站联系方式 | 加 admin token 鉴权 fail-closed,已配 `ADMIN_TOKEN`,验证 403 |
| `/api/order` 金额前端可篡改 | 加金额范围校验 + 限流 |

---

## 🔴 仍需处理的 P0(分三档)

### 档1 · 需你拍板(架构/产品决策,决定怎么修)
1. **付费墙形同虚设**:所有算命端点无鉴权,`curl` 直接拿完整万字付费报告,前端只 CSS 盖了个"锁"。**收入漏光。** 修法需定义 basic/full 免费边界 → 未付费只后端生成/返回基础维度,完整维度校验订单后才给。**这是产品决策。**
2. **架构分裂**:`vercel.json` 把 API 配成 serverless,前端相对路径 `origin+/api` → 走 Vercel 域名时 API 在 serverless(我的 JSON 落盘失效、多实例"付款成功拿不到货"),走 HK 域名才走 PM2(落盘有效)。**必须二选一定死**:①统一到 HK PM2 单进程(落盘方案有效,最省)②Vercel + 外部 DB(复用 Slim 的 Supabase)。

### 档2 · 需真实内容(合规红线,不能编造)
3. **隐私政策/条款/退款页** 真内容(现在 footer 全 `href="#"`)+ 收集生日前的同意勾选 → 韩国 PIPA / 中国个保法强制,不挂=违法。
4. **报告 AIGC 标识**(AI 标识办法)+ **整份统一免责**(现在 14 页报告无统一免责,却有"别辞职别创业/结婚运最强年"等人生指令)+ **健康页去医疗化**(删甲状腺/乳腺/子宫具体筛查,准医疗越界)。

### 档3 · 韩国从样张到上线(工程接线项目,不是"再优化样张")
5. 7 个死按钮接真动作 / 建韩语免费结果页 / 报告参数化 + 韩语八字引擎(后端有真排盘引擎 `bazi.js` 可复用,但 prompt 是简体中文需韩化+结构化)/ 接韩国支付(KakaoPay等)/ 4 个韩语法律页。

---

## 🟡 P1(放量前修)
- 15+ LLM 端点零鉴权零限流 → 可刷爆 DeepSeek 额度(记忆里 generate-bg 烧钱漏洞的翻版,这次 15 个)
- 未成年硬门(现在只"권장 19세")
- 小红书:报告里开运水晶/择日/风水**不得**直接发公开图文(封建迷信下架风险),只发已过审科普/情感/祭祀
- 退款条款按韩国电商法写(现"未阅即可退"可能违反撤回权)
- 社会证明数字/五星好评上线初期须真实(标示广告法)

## ✅ 守住的(做得好)
- 麦玲玲 IP 红线(全线无名号)
- 代烧"不承诺改命"、逝者克制
- 营销包五红线 + 发布前合规勾选门
- 报告/落地页/卡片 设计与韩语质量(9.0-9.6)

---

## 一句话给 CEO
**设计和内容做到了 10 分,但"能安全收钱 + 合规上线"是另一层。** 中文主站的付费墙和隐私是**现在上线就漏钱+违法**的两条,必须先修。韩国那套是精美原型,离上线还有一个"接线+本地化+合规"周期。建议:先修中文主站付费墙+隐私+架构(能真上线收钱),韩国按 Phase-0 原型推进、预留后端接线期。

*三份完整审计原文见对话记录。审查:善缘 CEO(Claude)· 2026-07-30*
