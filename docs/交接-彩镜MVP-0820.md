# 交接 · 彩镜个人色彩测试 MVP（0820）

## 一句话
免费「拍脸测四季色彩型」Web 工具，**已上线** `https://mylumee.cn/caijing`。纯浏览器跑（MediaPipe），零后端、零 API、每用户≈¥0。靠淘宝联盟带货变现。

## 核心文件
- `samples/caijing/viral/color-webapp.html` —— 单文件全部逻辑
- `samples/caijing/viral/vendor/` —— MediaPipe 模型/WASM/qrcode/html2canvas（35MB，**大陆访问必须本地托管**，未入 git，重下见文件末）

## 已实现
拍脸 → 眼白白平衡取色 → 规则分类春夏秋冬 → 出**四季扇形卡**（脸放四季色扇上，你那型带皇冠★，唯一晒图）+ 真上身换衣（多类别分割只给衣服换色）+ 彩妆三件套 personal-shopper（口红/眼影/腮红按实测色匹配色号→淘宝带货）+ 真二维码（指 mylumee.cn/caijing?ref=）+ `?ref=博主` 追踪 + PostHog 埋点框架（待接 key）。

## 部署
- 大陆服务器 `8.160.175.232`（key `~/.ssh/xinshen`），文件在 `/var/www/caijing/`
- Caddy `/etc/caddy/Caddyfile` 已加 `handle /caijing/*`（备份 `Caddyfile.bak.caijing`）
- 更新方式：`scp color-webapp.html root@8.160.175.232:/var/www/caijing/index.html`

## 待办（只有 Karen 能做）
1. **淘宝转 3 个口红推广链接**（pub.alimama.com「立即推广」→ s.click 短链）→ 填进 `LIP_PRODUCTS` 的 `url`，佣金才结算（现是搜索兜底）。PID=`mm_4977728889_3345700413_116134000380`
2. **私信 5 个博主**发小红书（名单+话术 `docs/彩镜-种子博主名单+私信话术.md`，最有效=扒爆款评论区）
3. **接 PostHog key**（在 Lumee `app-rn/src/monitoring.ts` / 交接-0703 里，待确认 host）→ 加 init 片段看漏斗
4. **2 周看数据**：>200 测试/博主=GO 扩量；<50=停

## 红线/坑
- **光有 PID 不结佣金**，必须账号内「立即推广」转链
- **大陆必须 vendor 本地**（Google storage + jsdelivr 大陆封）
- **入口别收费**（杀病毒性）；要收在「出结果后解锁完整报告 ¥9.9」
- **别在分发验证前建 V2**（付费 PDF）

## 下一步方向
- 分发验证是唯一重点，不是加功能
- V2 付费完整报告 ¥199（镜框/珠宝/发型/穿搭/妆容**试戴图**）路线在 `docs/彩镜-V2付费版路线图.md`。**命门=AI 换发型/穿搭"上脸"能不能像本人不网红脸**，建之前先花 ¥20-50 测这个（花钱先报预算铁律）

## vendor 重下命令
见 `color-webapp.html` 里 `./vendor/` 引用；模型：MediaPipe face_landmarker / selfie_multiclass / selfie_segmenter（storage.googleapis.com mediapipe-models）+ tasks-vision@0.10.15 vision_bundle.mjs & wasm + qrcode-generator@1.4.4 + html2canvas@1.4.1。
