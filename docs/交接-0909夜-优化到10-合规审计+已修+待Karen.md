# 交接 · 0909 夜 · "优化到10" 合规审计 + 已修 + 待你拍板

> Karen 睡前指令"优化到10分"。我按 SOP:3 并行审计 agent(产审分离)→ 修清楚的红线 → commit/push **不夜里部署**。你醒来看 diff + 拍板部署 + 处理"待你"清单。

## 🔴 首要提醒:部署路径
我的修复都 commit 在 **`deploy-candidate-0907`**(commit `1603743`),但**服务器现在跑的是 `main`(07aa7ba)**。所以**这些修复还没上线**,要生效必须把 `deploy-candidate-0907` 合进 `main` 再让服务器 pull。Windows 机把 main 定为 canonical 了——**部署前先和它在留言板对一下,别两条线打架**。

---

## ✅ 我今晚已修(commit `1603743`·未部署)

编造数据是你反复画的绝对红线,我只删"确认编造、离散、删了不破页"的,全部 git 可回滚:

| 文件 | 改了什么 | 验证 |
|---|---|---|
| `gongfeng.html` | 删全部 **8 条编造具名证言**(含**怀孕/治病/超度救命**:陈太太"诞下男婴"、翁小姐"皮肤病痊愈"、谭先生"超度"等)。渲染改成"有 miracle 才显示",不留空框 | JS 校验 TEMPLES 8 庙 0 miracle ✅ |
| `life-events.html` | 删整个"真实案例/众多家庭的信任"证言区(张女士/李先生"病情好转出院"/王女士)+ 删"命理师平均从业18年"假统计 | div 开合平衡 ✅ |
| `seo-bazi.html` | 删"已有12,846位用户""准确率9.2/10"假数据 + 删"林小姐"编造证言 | div 平衡 ✅ |
| `hehun.html:556` | 同意行文字浅褐(215,211,196)配浅纸底看不清(对比1.29:1)→ 改深色(74,44,44)。**法律同意门必须可读** | ✅ |

---

## 🔴 待你拍板(涉及产品/翻译/整页内容·我不夜里擅动)

### A. life-events 两个服务本体涉生死疾病(产品决策·砍SKU)
- `life-events.html:181-191` **"病情推算"**服务:"推算病情转机时间…这一劫能否渡过…康复时间段" = 预测疾病预后。
- `life-events.html:207-210` **"临终时辰"**服务 = 预测死亡时辰(最高风险)。
- Hero/分享文案(`:138,:461`)含"病情、临终"定位。
- **建议:下线这两个服务,或彻底改写为"健康宫五行调养参考"去掉康复/生死承诺。这是砍产品,得你定。**

### B. gongfeng(供奉页)整页仍有疗效措辞 + 产品性质
- 庙史里"千年以来求子成功、疾病痊愈"、`wishes` 数组里"治病痊愈/慢性病好转/超度亡灵"(如 `putuo`/`emei`/`jiuhua`)。
- `gongfeng.html:239` "汇聚**最灵验**的道场" 最高级断言;`:269` 免责声明现在孤立了(灵验案例已删,那行可删)。
- **建议:去掉 wishes/庙史里的疗效词;并确认"代供祈福"作为付费产品要不要上、怎么合规。整页内容级,你定。**

### C. 7 个国际语言 LP 编造具名证言(要翻译)
`lp-master-ES-LATAM`(245-260)/`lp-master-PT-BR`(410-437)/`lp-master-TH`(255-269)/`lp-master-IN`(284-300)/`lp-master-KR`(498-528)/`lp-bazi-th`(284-297)/`lp-in-vedic`(288-315) —— 全套编造用户+"准得吓人"好评。
- **合规做法已有样板:`lp-bazi-en-b.html:1353` 明确标 "Illustrative Examples · not real users"。照此改写(需各语言翻译),或删除。**

### D. 其它假社会证明(小)
- `lp-bazi-th:168`(28,459人/4.9)、`lp-in-vedic:285`(4.9/14,520)、`lp-tarot-cn:9`(meta 10万次)、`yinzhai:322`(千余户)、`report-demo:133-145`(匹配度95%+年薪¥15-25万,建议标DEMO)、`bio-cn:216`/`bio-kr:186`(限量100份)、`member.html:377-383`(每访客倒计时"优惠失效")—— **若无真实机制则删数字/删倒计时。**

---

## 🟡 英文页设计/定价(转化审计·归你的独角兽落地)
- 英文首页产品少/无价格、合婚英文页币种没标 USD、免费/付费边界不清。⚠️ 注意线上英文首页是 **`home-en.html`**(Caddy rewrite),不是 `en.html`——审计看的是 en.html,要对到 home-en.html 核实。
- 这些是设计判断,按"设计先行拍板"归你。

---

## 部署命令(你审完 diff 满意后)
```bash
# 1) 把修复合进 main(先和 Windows 机对齐,别撞)
# 2) 服务器 pull + 重启
ssh root@47.242.80.65 'cd /opt/shenyuan && git fetch origin --quiet && git reset --hard origin/main && pm2 restart shenyuan'
# 纯前端改动(本批全是pages/)其实可不restart,Caddy/node直接读文件
```
回滚:`git reset --hard 1603743^` 或按需回退。

## 审计原始结论(供追溯)
3 份审计(合规假数据 / UI对比度 / 移动端转化)已跑完,本文档是合并+已执行版。合规审计判定全站 P0 = 5 页 3 类问题,`gongfeng`+`life-events` 最高危(已处理证言部分,服务/措辞留你)。
