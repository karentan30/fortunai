# Runae 交接 · 0903（报告"一眼假"P0 修复 + 战略:产品封板转分发）

> 代码已 push origin/main（commit `2608598`）+ 服务器 `git reset origin/main` + `pm2 restart shenyuan` + health 200。

## 一句话
独立复审发现 5 个方法的免费总览报告**全部"一眼假"（5.5/10，没有一个能干净收钱）**，定位到两个系统性根因并全修，已部署，第二轮复验进行中。

## 做了什么（P0 信任红线修复）
根因 1 · **付费 prompt 自相矛盾**：真实性铁律禁数字，同一份 userPrompt 却写"多用量化(年份/百分比/评分)增强说服力"→ 主动逼 LLM 造假。八字（divination.js 付费 userPrompt）+ session 两处已删，改为"靠具体场景+真实干支/大运年份增强代入感"。
根因 2 · **`_dropFakeStats` 兜底只拦 %**：漏掉 `倍/秒/天/第X版/pt/几成/近X年最`。已扩正则（数字+中文数字都拦），**单测验过真引擎数据不误伤**（"大运2021-2036""五行木2.3"原样保留）。
根因 3 · **prompt 治本**：八字 + session 真实性铁律补漏网写法禁例（3倍/0.3秒/三秒/第7版/高于七成）+ **新增"禁虚构研究论文来源"**（堵西占编《占星与神经科学》那种）+ 编造往事新例（"近五年你已试过""帮朋友后引荐客户"）。

改动全在 `server/routes/divination.js`。复审真实报告用 SSH+curl `localhost:3021/api/session`（免费 overview 无需付费门）。

## 待办
1. **等第二轮复验出分**（上轮 5.5，目标 ≥9 才放开收钱）。deepseek 对紫微较顽固，留意回潮。
2. **付款 E2E 逐方法自测**（6 方法：八字/紫微/西占/九星/合婚/手相）：重点验 ③金额对(单$3.99/全套$11.99) ④付款成功回跳 ⑤回跳真解锁 ⑥锁定内容不进 DOM(防白嫖) ⑦刷新不丢。目前只有八字真机点通过。
   - 可我用浏览器自动化跑，**前提先确认 runae.net 是 Stripe 测试模式还是 live**（live 会真扣钱，只能手动）。
3. 报告配图（命盘/四柱/五行图样张已建未接进 session，零预算 Chrome 渲染可补）——非上线阻断，后补。

## 红线 / 坑
- **`_dropFakeStats` 是兜底不是治本**：正则洗词会留残渣（"高达XX%"→"相当比例"但捏造的论文出处整句还在，反而更假）。长期必须靠 prompt 层不生成，正则只补漏。
- **"prompt 自相矛盾"是本次根因模式**：以后加 userPrompt 别再写"多用量化/数据/评分"这类跟真实性铁律打架的话。
- **正则勿误伤真引擎数据**：大运年份区间、五行小数、西占度数是合法的，改正则务必单测这几类不被吃掉（本次已验）。
- 关联既有地雷 [[reference_engine_injection_field_mismatch_landmine]]（注入真引擎字段名读错→空注入→LLM 瞎排）。

## 战略结论（本 session 想清楚的，影响下一步）
- **"AI 面相读性格/上传照片"= 红海**：Sima/PhysiognomyAI/faceMatch/SciMatch 已做，连"商业合作合盘""不用生日"都被占了。零壁垒，别单独立项、别做新 App。
- **面相三张图实测暴露产品真身**：它不是"面相算命"，是"AI 读微表情+体态反推印象"——同一个人不同照片会读出相反性格（①一致性是命门）。若做，只能当 **Runae 引流子页**（复用现成面相管线 + 确定性特征提取锁一致性），不投人力。
- **Runae 功能已够，真缺口是 distribution（≈0 用户）**。所以：**产品侧封板（别再加功能），精力全转分发**。

## 下一步
1. 复验达标 → 付款 E2E 点通 → Runae 正式"能收钱"，产品线**封板**。
2. 转分发实验：面相钩子 / AI 掌纹图小红书 7 天冷启动（钩子已想好 3 条：#相亲"奔结婚还是玩玩" / #"这4种面相别跟他合伙" / #合伙"别跟他碰钱"；方案见 `docs/获客-AI掌纹图-小红书-0820.md`）。
