# Runae 真机走查 Bug 单 · 0908（Karen 亲测·崩溃会话丢失后重建）

> 背景：0908 上午 Karen 在一个会话里真机走查 Runae 发现多个 bug，机器崩溃后那会话丢了、没提交。本单据 Karen 重新口述重建。**昨天(0907)的修复已上线，本单是今天新发现、多数未修。**

## 🔴 P0 — 设计/合规

### D1. 独角兽设计只在中文页，英文页没有
- **现象**：`2f139f2「6方法独角兽重设计」` 只改了中文页（bazi/ziwei/tarot/mianxiang/astrology/daily）。**英文页（bazi-en/ziwei-en/hehun-en/mianxiang-en/lp-tarot-en）从来没做独角兽版**，还是旧版。
- **要求**：英文页要对齐独角兽设计。
- ⚠️ **设计判断类，先渲染截图给 Karen 拍板，不擅自重写**（守"设计先行 + 禁框套框"）。

### D2. 中文页现在"都是长方形框、一长条"，不像独角兽了
- **现象**：中文页虽然提交了独角兽重设计，但 Karen 实测看到的是**一长条方框堆叠**，独角兽感被压平（疑似后来的浅色主题/改动回归成方框）。
- ⚠️ 同上，先截图对比，Karen 确认再改。

### D3. 很多假数据（整个 Runae·合规红线）
- **现象**：多个页面仍有"一眼假"的编造数据（假百分比/评分/幸运日/讨论区/倒计时/具名证言等）。
- **要求**：全站 grep 清查并清除编造数据（合规红线·反复回归）。**具体哪页哪段待 Karen 补充**。

## 🟡 P1 — 具体 bug

### B1. 出报告前"同意"那行看不清 ✅根因已定位
- **文案**："我已年满18周岁，同意将出生日期/性别用于命理分析…（隐私政策）"
- **根因**：`bazi.html:481` `.consent-row label { color: var(--ink-2,#3d453a) !important; }` —— `#3d453a` 深色 + `!important` 盖过了行内浅色，深字配底=看不清。
- **修**：改成与所在主题背景对比足够的颜色（该页是浅纸主题就得确认深浅关系）；**同类页(astrology/hehun/lp-bazi-cn等含同意行的)一起查**。

### B2. bazi 桌面版整页无法上下滑动 ✅已修已上线
- 根因 `bazi.html` 桌面媒体查询 `body{overflow:hidden}` → 改 `visible`。commit 91ebe95，已部署。**其他5页已查无此问题。**

### B3. 裂变图做不完 / 裂变相关未完成
- Karen 提到裂变的图做不完。**具体卡点待补充**（是生成流程、还是某页裂变模块 bug）。

## 待 Karen 补充
- D3 假数据：具体在哪个方法页、哪一段？
- B3 裂变：具体指什么？

## 修复方式
- 清晰 bug（B1 同意行对比、D3 假数据）→ 直接修 + 部署（生产分支 `deploy-candidate-0907`）。
- 设计类（D1 英文页独角兽、D2 中文页方框）→ **先渲染截图 → Karen 拍板 → 再改**。
