# 交接 · Runae（2026-09-13）

> 承接 `docs/交接-0911.md`（那份讲 0911 的安全/合规基建）。本轮只讲**钱路**：能不能收、
> 收了能不能发货、页面上写的价是不是真扣的价。细节看 git log（`c1dc0bc..d25ba27`）。

---

## 1. 这轮做了什么（三句话）

1. **堵住「没人能收到货」的根**：三条下单通道原先允许匿名建单，而发货只认 `user_id`，
   匿名订单＝钱收了、没有任何人能解锁。现在报告类商品没登录直接 401 + 前端带表单快照
   去登录页、登录完回原页接着付。
2. **阴宅/风水接上真付费**（你 0913 批准的那条）：阴宅 $99 / ¥299、风水 $29 / ¥59，
   含国内微信/支付宝扫码。修完对抗审计查出的两处断点（见第 2 条）。
3. **价格要一致**：15 个页面 + 目录改成「页面写的 = 实际扣的」，一律以实收为准改展示，
   没有动任何人的钱。

## 2. 对抗审计查出、已修的（每条都有实证，不是推测）

| 问题 | 后果 | 状态 |
|---|---|---|
| 令牌解析「第一个非空胜出」 | body.token='guest'（hehun-KR 就写着）或改密后残留的旧 Bearer 顶掉有效 cookie → 已登录用户被判匿名、报告不解锁、下单 401 | 已修为「逐个来源查库、查到用户为止」 |
| 阴宅页用 localStorage 判登录 | 登录只发 httpOnly cookie，localStorage 永远空 → **付过钱的用户也弹付费墙，再点解锁再收一次 $99** | 已删该判定，登录态只由服务端判 |
| 阴宅回流生辰键名写错 | 存 `masterBirthYear`、读 `masterYear` → 付完钱停在空表单，报告不重跑 | 已对齐 |
| 风水回流静默丢数据 | 家庭成员/装修状态/照片整页重载后全没，$29 的报告缺照片且无提示 | 成员+装修状态已回填；照片改为明确提示重传（不自动重跑，免得产出缺照片的报告） |
| 7 个报告页没引 common.js | 未登录被 401 后只有一句 "Payment error"，没有登录入口 | 已补 |

## 3. 生产事实（这轮新查的）

- **生产环境确有 Stripe live 密钥**，会员三档走的是 payment.js 里**硬编码**的固定价，
  已在 LIVE 账号上只读核对：monthly $9.90 / quarterly $24.90 / yearly $69，三个 price 都 active。
  也就是说「PRODUCTS 是唯一价目表」对会员不成立 —— 改会员价必须两边一起改。
- **64 张订单全部 `user_id = null`**，含唯一一笔 `completed` 的 $19（2026-09-08）。
  生产 device 上 `users=0 / tokens=0`，日志里 0 条 `[AUTH] Register/Login`：
  **历史上从来没有人持有过账号**，所以每一笔付款都没人能解锁。这轮改动只防新增，旧账要你拍板（第 5 条）。

## 4. 待办 / 已知没修

- **国内通道断头**：还有 10 个中文报告页（report-daily / report-astrology / report-tarot /
  report-palm / report-hehun / report-face / report-v2 / advisor-chat / booking / palm-reading）
  点了只弹「微信支付宝接入中」。要给它们接上跟我给风水/阴宅做的一样的扫码通道。
- **死按钮 7 处**：duanshi（alert 即将开放）、jieqian（TODO + alert）、shengxiao（无 onclick）、
  lp-zh-main / lp-in-vedic（跳 `server/pay`，那是个模块不是路由，必然 404）、
  best-timing-kr（demo alert）、avatar（$19.90/张，无 SKU）。
- **en.html 两张卡片在卖「收不了钱的页面」**：ziwei-en.html 与 fengshui-runae.html 全页
  没有任何支付端点，卡片却标着 $19.90 —— 要么接上 SKU，要么把价格撤掉。
- `duanshi_full`（$29）在目录里但不在任何解锁表里 → 哪天把那个按钮接上就是「收钱不解锁」。
- cnPay 扫码轮询 2 分钟超时后没有「我已经付了」的补救入口，再点会建新单（重复收款风险）；
  二维码图用的是 `api.qrserver.com`，国内网络可能不可达。

## 5. 需要你拍板（就两件）

1. **那一笔真实的 $19**（`daily_companion_year`，2026-09-08 完成，Stripe session `cs_live_b1t0…`）：
   退款，还是从 Stripe 查出付款邮箱、建账号并把权益补给他？（我倾向后者，一句「我们找到你了」比退款值钱。）
2. **要不要给旧订单开个「凭邮箱认领」入口**，而不是一律登录墙。现在改法是硬登录墙
   ——安全，但把「先付款再注册」这类用户挡在门外了。

## 6. 红线（没变）

- 生成类付费 key 仍锁在 `~/.config/karen-paid-keys.LOCKED.env`，不自动 source；花钱先报预算。
- 用户界面不出现具体模型名。
- 部署走「本地 commit → 服务器 `git reset --hard origin/main` → 备份 `server/data.json`（gitignore，不在 git 里）→ `pm2 restart shenyuan`」。
