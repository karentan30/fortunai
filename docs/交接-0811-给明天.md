# 善缘 · 交接文档 2026-08-11

## 做了什么

| 模块 | 改动 |
|---|---|
| LLM切换 | DeepSeek→Qwen(DashScope) · 阿里云内网endpoint · 白天无2x溢价 |
| 聊天页 | `pages/chat-magic.html` 神秘风重设计 · JS逻辑保留 |
| CMO剧本 | `docs/CMO-三条主推剧本-0811.md` 三条TikTok+小红书脚本 |
| Agora | `agora-access-token ^2.0.4` 修好 · 视频通话路由已启用 |

## 待办（优先级排序）

1. **P0** 预览 `chat-magic.html` → 满意后 `cp pages/chat-magic.html pages/chat.html`
2. **P0** Karen 真机走付款流程验证（微信¥9.9全链路）
3. **P1** 将 `bazi-ziwei-skill` 算法层接入后端 `/api/bazi` — 本地排盘替代LLM，省80%token
4. **P1** 法律页面填 Capstone 公司真实信息（Karen自己填）
5. **P2** 合婚页 `hehun.html` CMO线路B接流量

## 红线 / 坑

- **Qwen key** 在服务器 `/opt/shenyuan/server/.env` → `DASHSCOPE_API_KEY`，本地无此key → 本地测试AI功能会报错，属正常
- **DeepSeek余额=0**，不要改回去，Qwen已验证可用
- **agora key** 在 `.env` 的 `AGORA_APP_ID` / `AGORA_APP_CERTIFICATE`，视频通话功能依赖这两个值
- **Vercel限额** — 100次/天，改完攒一批再统一部署，别每改一次就push到vercel

## 服务器

```
SSH: ssh root@47.242.80.65
进目录: cd /opt/shenyuan/server
重启: pm2 restart shenyuan
查日志: pm2 logs shenyuan --lines 50
```

## 下一步推荐

出报告卖 → 跑CMO线路A（八字钩子→$9.90报告）投流测ROI → 有数据再加人
