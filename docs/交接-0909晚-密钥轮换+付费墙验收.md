# 交接 · 2026-09-09 晚 · 密钥轮换 + 付费墙验收

> 本轮只碰安全/密钥/协作,没动 Runae 业务代码。所有 commit 已 push。

## 做了什么(本轮全部 commit / push)

**git 提交**
| 仓库 | 分支 | commit | 内容 | 我做的? |
|---|---|---|---|---|
| fortunai | deploy-candidate-0907 | `3946d69` | `docs/密钥轮换-runbook.md`(纯流程零密钥值) | ✅ 是 |
| fortunai | deploy-candidate-0907 | `9d1c906` | 打码交接doc里 sk/pk_live 占位符引用 | ❌ 另一会话(同"karen"身份)·已push·**核实非真泄露**(历史里无真key值,只是占位符前缀) |
| lumee-docs-shared | master | `803c2f6` | 留言板 Mac→Win:ADMIN_TOKEN已轮换+webhook轮换中+请真单回归付费墙 | ✅ 是 |
| lumee-docs-shared | master | `e1f898c` | 留言板 Mac→Win:test key已放服务器路径 | ✅ 是 |

**服务器改动(非 git·值不进库/不进聊天)**
- **ADMIN_TOKEN 已轮换**:`/opt/shenyuan/server/.env`,新值 openssl 现生成。实测旧 token→`/api/ab-stats` 401、新→200、pm2 online。旧值备份在服务器 `server/.env.bak.*`(gitignore)。
- **Stripe test key 交给 Windows 机**:从本机 `~/.config/karen-paid-keys.LOCKED.env` 灌到服务器 `root@47.242.80.65:/root/stripe-test-keys.env`(chmod 600 仅root)。给它做付费墙真单回归用。

## 待办
1. 🔴 **Stripe webhook secret 轮换收尾**(等 Karen):Stripe 后台 Live 模式 roll → 新 `whsec_` 存 `/tmp/whsec.txt` → 叫 Mac 会话灌进服务器+重启+验证(步骤见 runbook B)。
2. 🔴 **付费墙真单回归**(Windows 机做·已给 test key 解锁):跑测试支付生成真订单,验 `tarot_5`($19.9)/`tarot_3`($9)/`saju_kr_full`(₩9900) 三个 SKU 的真付费用户拿到完整解读非预览。只有预览就回滚(它已记好回滚点)。这条一绿,收钱链路才真闭环。
3. 🟡 假数据全站清查 + 英文页独角兽设计落地(体验层,不拦收钱)。

## 红线 / 坑
- **密钥值永不进 git / 永不贴聊天**(这次 `.env.bak` 事故的教训)。共享靠"两台都 SSH 同一台服务器",不靠传值。轮换流程见 `fortunai:docs/密钥轮换-runbook.md`。
- **另一会话也在 fortunai 提交**(同 `karen` 身份,如 9d1c906)。改 fortunai 前先 `git pull`,避免撞车。
- **ADMIN_TOKEN 已变**:任何用旧 token 的测试/脚本/云routine会 401。要新值 `ssh root@47.242.80.65 'grep "^ADMIN_TOKEN=" /opt/shenyuan/server/.env'`。
- **付费墙已修已上线**(Windows 机 09-09 部署):后端切分,匿名只得预览+`locked:true`。别以为还在白送——已止血,只差真单回归那一步。

## 背景(Runae 上线整体·非本轮但相关)
- 服务器跑 `main`(07aa7ba,09-09 归位),0908/0909 修复全已部署(英文报告出中文、独角兽设计、绿圆环首页、合婚真算、付费墙止血均在线上)。
- 生产事实源见 `docs/交接-0907最新-已上线状态.md`;两台协作见 `~/projects/shared/协作/留言板.md`。
