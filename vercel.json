{
  "version": 2,
  "name": "shenyuan",
  "framework": null,
  "buildCommand": null,
  "outputDirectory": ".",
  "_comment": "架构统一到 HK PM2(47.242.80.65:3021): API 只在 HK 单进程跑(JSON落盘持久化有效)。Vercel 不再跑 API serverless(已移除 functions/api路由), 防多实例导致付款成功拿不到货。生产入口用 HK 域名; 若 Vercel 仅托管前端, 前端需将 API 指向 HK 的 https 域名。",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
