# 善缘交接 · 上线安全批次 0817

**一句话**:核心产品全量上线 + 两语言诚实红线清零到可投付费流量,监控接通。生产 = `898ce90`,PM2 online,Qwen 降本生效。

---

## 做了什么(本轮)
- 专业排盘引擎替代 LLM 自排(生产实测四柱正确)· LLM 换 Qwen 降本
- 两语言 launch-safe:专家两轮复审,中文 6.0→达标 / 英文 6.5→9
- 新增 `/api/hehun/preview`(真日主五行生克基础分 + 诚实点评,零 LLM,中英双语)
- 清零红线:合婚假哈希、编造证言、假用户数/假JS计数器/假实时弹窗/假紧迫名额(核心页+8落地页,净删 935+ 行)、假精准承诺
- 价格全站统一会员 $9.90/月 · 首页 geo 强跳改可关闭横条(不再弹走海外华人)
- Sentry 监控走 Lumee 中台(装 @sentry/node,DSN 内置 shared/monitoring.js,project=shenyuan 标签)

## 待办 · Karen 手上
- ⚠️ 确认 `hi@lumee.cn` 真能收信(法律页官方联系点,隐私请求有法定回复时限)
- ⚠️ 合婚锁卡"3个吉日" vs 报告"最佳婚期吉年"措辞有出入 → 选 A 收紧文案 / B 加强 prompt(**未决**)
- PortOne 问"香港主体能否结算境外" · 韩文母语校正两条腿招(¥6-10K) · 韩国=Stripe 跨境(本地渠道无韩国主体开不了,放弃)
- 5 决策落地(专家已拍板:英文文案C组微调 / 大运标准10十神3档 / 预算保守分两段闸)

## 红线坑(教训)
- **vendored 依赖防 gitignore 误伤**:新排盘引擎 dist/ 被全局 `dist/` 规则忽略,只提交了 package.json,服务器缺库启动崩溃 → 部署前必须在纯净环境验证启动,别在挂掉的生产上调试(本轮崩过一次,1分钟回滚恢复)
- **Qwen 一旦配 key 强制走 Qwen 不降级 DeepSeek** → 端点已改公网可配置(`QWEN_BASE_URL`),VPC 内网端点善缘服务器不可达
- 付费 key 一律 Karen 手动、服务器面板填,不进 git/不贴聊天

## 下一步(可选)
- 合婚"3吉日"口径定 A/B → 顺手改
- 若投放落地页超出已清 8 页,新页需同样清假证言

## 关键位置
- 部署:`ssh root@47.242.80.65` → `/opt/shenyuan`,PM2 `shenyuan` 端口 3021
- 合婚真基础分/点评:`server/routes/divination.js` `/hehun/preview`
- LLM 封装:`server/lib/llm.js`(Qwen/DeepSeek 自动切)
- 监控中台:`~/projects/shared/monitoring.js`
